# 引き継ぎ — リベシティ スキルマーケット出品

最終更新: 2026-08-31

このファイルだけ読めば作業を再開できるようにしてある。
設計と実測の根拠は [`docs/superpowers/specs/2026-08-14-libecity-skill-market-design.md`](docs/superpowers/specs/2026-08-14-libecity-skill-market-design.md)、
出品原稿は [`listings/`](listings/) にある。

---

## 1. いま何をしているのか

リベシティ スキルマーケット（`https://skill.libecity.com`）への出品。

| 項目 | 内容 |
|---|---|
| **目的** | **受託案件の入口**。スキルマーケット単体の売上が目的ではない |
| 出口（受けたい案件） | ①中小企業の業務システム開発 ②事業者向けWebサイト＋AIチャットボット ③AI業務自動化の導入・伴走 |
| 稼働 | 週20時間以上 |
| 対応可能時間 | 平日 20:00〜23:00 ／ 土日祝 9:00〜12:00・13:00〜17:00・20:00〜23:00 |
| アカウント | イルカ会員。表示名「なおたか@沖縄の業務システム屋×AI」 |

**いま立っている場所**: 出品6件が公開中。うち3件を 2026-08-31 に新規出品した。
まだ取引0・やりとり0。次は**反応の測定**と、**受注が来たときに納品できる状態を作ること**。

---

## 2. 絶対に守る制約

### 規約: プラットフォーム外で受注してはいけない

利用規約（`/pages/rule`）の禁止事項:

> 出品者が利用者投稿情報によって、自己の商品やサービスあるいはSNS等メディアへ誘導し、
> **本サービスの機能を通さずに仕事を受託しようとする行為**。
> （対象サービスの補足説明のために外部メディアのURLを掲載する場合はこれに該当しない）

→ 高単価案件もスキルマーケット上の出品として受注する。これは 37162（業務システム開発）で担保済み。
補足説明として `*.nexeed-lab.com` のURLを載せるのは明示的に除外されており問題ない。

その他: 既製品・汎用品の売買は禁止。リモートで完結するサービスのみ出品可。

### 出品文には my-data で裏の取れる記載だけを載せる

**これが最重要。** 過去にAI生成した出品文に事実と異なる記載が混入していた（2026-08-14 に是正）。
技術名・実績・資格・数値は、ひとつずつ `../my-data` と突き合わせる。
裏付けが無ければ**勝手に補わず本人に確認する**。

今回の3件も全文を `my-data` と突き合わせて書いた。顧客名は出していない（`SENSITIVE.md` 方針）。

### 出品文に実数を散りばめない

数値は各商品につき1〜2個に絞り、あとは検証可能なURLを置く。増減で更新漏れが出るため。

---

## 3. 現在の状態（2026-08-31 実測）

### 公開中の出品（6件・下書き0）

| ID | タイトル | 価格 | 閲覧 | 気になる | 備考 |
|---|---|---|---|---|---|
| [37162](https://skill.libecity.com/services/37162) | 【無料相談】テスト付きで業務システムを開発します | 150,000円 | 0 | 0 | **今回出品**。設計⑥ |
| [37161](https://skill.libecity.com/services/37161) | サイトの更新を毎日見張ってLINEに通知します | 10,000円 | 0 | 0 | **今回出品**。設計B |
| [37160](https://skill.libecity.com/services/37160) | 毎朝のPC作業を無人化｜定時実行の仕組みを作ります | 15,000円 | 0 | 0 | **今回出品**。設計A |
| [34217](https://skill.libecity.com/services/34217) | 【診断書納品】AIでやる事・やらない事を診断 | 5,000円 | 9 | 0 | 2026/03作成、08-14に全面書き換え |
| [34218](https://skill.libecity.com/services/34218) | 問い合わせメールのAI自動返信を構築します | 20,000円 | 8 | 0 | 2026/03 |
| [34120](https://skill.libecity.com/services/34120) | 毎日の作業をAIで丸ごと自動化 | 30,000円 | 16 | 1 | 2026/03 |

**6件とも画像は同じ型で統一済み**（2026-08-31）。原稿と画像の対応:

| 出品 | 原稿 | 画像 / pptx |
|---|---|---|
| 37160 | `listings/02-teiji-jikko.md` | `02-teiji-jikko` |
| 37161 | `listings/03-henkou-kenchi.md` | `03-henkou-kenchi` |
| 37162 | `listings/04-gyomu-system.md` | `04-gyomu-system` |
| 34217 | `listings/01-ai-shindan.md` | `01-ai-shindan` |
| 34218 | （原稿なし・サイト上が正） | `05-mail-ai` |
| 34120 | （原稿なし・サイト上が正） | `06-sagyo-jidoka` |

### 今回やったこと（2026-08-31）

1. **市場を再実測**（Claude in Chrome）。結果は設計書 2.6節に記録
2. **商品を差し替え**。設計②（Web技術診断書）と③（Windows×Claude Code）は見送り、
   A（定時実行の自動化）とB（サイトの変更検知）を新設
3. **原稿を作成** — `listings/02-teiji-jikko.md` / `03-henkou-kenchi.md` / `04-gyomu-system.md`
4. **サムネイル3枚を作成** — `listings/assets/*.png`（1320×880 = 3:2）
5. **3件を出品** — 37160 / 37161 / 37162
6. **サムネイルを全面的に作り直した**（1回目の濃紺・文字だけの画像は一覧で沈むため）。
   上位競合のサムネイルを実測 → 明るい背景・イラスト入り・太い見出しへ。
   PowerPoint（`listings/assets/pptx/*.pptx`）で組み、LibreOffice で PNG 化して差し替えた
7. **この作り方を標準として固定した。** 本人が「めっちゃいいです。これからこの形で」と承認。
   `listings/assets/README.md` の先頭に明記し、記憶（`libecity-thumbnail-workflow`）にも保存した
8. `node_modules` を誤ってコミットしたので `.gitignore` を追加して除外した
9. **古い3件（34217 / 34218 / 34120）の画像も同じ型で作り直して差し替えた。**
   差し替え前の画像は `listings/assets/legacy/` に退避済み（34218・34120 はリポジトリに無かったため）
10. **本人の指示で、6枚すべてから金額を削除して作り直し、再度差し替えた。**
    価格は出品ページ側に表示されるため、画像には持たせない方針

### 検証済みの事実（実際に画面に出した出力）

- サービス管理画面で「すべて(6) / 下書き(0) / 出品中(6)」を確認
- `/services/37160`・`37161`・`37162` を XHR で取得し、いずれも **status 200**
- 3件の本文に、過去の事実誤認リスト（Railway / Zapier / Power Automate / Airtable / Java SE 8）が
  **含まれないことを確認**（`Notion` は37160の「関連サービス」欄に出る他人の出品タイトルで、自分の記載ではない）
- `search?parent_category=7&sort=new` で **37162 / 37161 / 37160 が上位3件**に並ぶことを確認
- サムネイル3枚を目視確認。日本語の豆腐化・プレースホルダ残りなし
- 金額なし版への差し替え後、6件すべての `/services/<id>` を取得して **status 200**、画像IDが
  114222〜114227 に入れ替わっていることを確認。1つ前の 114213〜114221 系はいずれも残っていない
- 公開中の実物（1320×880 PNG）をダウンロードして目視確認。金額の表示なし
- 公開されている実物（`storage.googleapis.com` の 1320×880 PNG）をダウンロードして目視確認
- **既存出品の書き換えでは新着枠に戻らない**: 新着36件のIDは36410〜37152番台で、
  34120/34217/34218 は1件も含まれなかった。34217 は08-14の書き換え後17日で閲覧 7→9（+2）

### 未検証のもの

- 新規3件の反応（まだ出品直後。閲覧0）
- 「つぶやき」への掲載効果 — **完了画面で勧められたが今回は載せていない**（本人の判断待ち）
- 商品A・Bの納品物の実体（受注が来たときに作れるかは未検証）

---

## 4. 次にやること（優先順）

### 4.1 納品物の実体を用意する ← 最優先

**3件とも「作って納品する」約束をしているが、雛形がまだ無い。**

| 出品 | 用意するもの |
|---|---|
| 37160 定時実行 | `nexeed-ops` を汎用テンプレート化（lib/wait・notify・lock・shot を切り出し、tasks/ に1本追加する手順書） |
| 37161 変更検知 | うちなーマネーの `scripts/monitor-sources.mjs` を、監視対象を設定ファイルで差し替えられる形に切り出す |
| 34217 診断書 | **PDF・A4で10ページ前後の診断書テンプレート**とヒアリングシート（Googleフォーム）。設問は出品の依頼テンプレート8問を流用できる |

### 4.2 反応を測る

**測定の起点は 2026-08-31、閲覧0から。** 2週間後（2026-09-14 目安）に判定する。

| 症状 | 原因 | 打ち手 |
|---|---|---|
| 閲覧が伸びない（2週間で50未満） | タイトル・画像・カテゴリ | タイトルを書き換えて再測定 |
| 閲覧はあるが気になるが付かない（200超で5未満） | 価格・内容の説得力 | 価格改定 or 納品物の明確化 |
| 気になるは付くが問い合わせが来ない | 依頼への導線 | 無料相談の入口を明示 |

物差し（同カテゴリ上位、2026-08-31 実測）:
三河乃風 10,000円で閲覧926・気になる64 ／ メケ 100,000円で865・34 ／ いまお 30,000円で692・31。

### 4.3 追加出品は「いったん不要」（2026-08-31 に本人が判断）

出品を増やす案を出したが、**本人が「やっぱり不要」と判断した。再提案しないこと。**
調べた結果は捨てずに残す。増やすとなったときはここから再開できる。

**IT・プログラミングの子カテゴリ件数（2026-08-31 実測）**

| 子カテゴリ | 件数 |
|---|---|
| 作業自動化・効率化 | 405 |
| AI活用・開発 | 161 |
| Webアプリ | 44 |
| モバイルアプリ | 12 |
| Mac・Windowsアプリ | 11 |
| サーバー・インフラ | 11 |
| **バグチェック・テストプレイ** | **3** |
| システムアーキテクチャ | 2 ／ ゲーム 1 |

- **バグチェック・テストプレイが事実上の空白**（3件・最高8,000円）。
  `qa-status.md` の監査実績（実バグ3件発見）がそのまま裏付けになり、納品物の手順も既にある
- **サーバー・インフラも薄い**（11件）。「サーバー代」検索は4件のみ。Cloudflare 移行（設計④）を置くならここ
- **入らない方がいい領域**: 「LINE公式」138件（キッシュ 22,000円が気になる150・閲覧1638。マーケター勢の陣地）、
  「補助金」21件（上位は中小企業診断士・士業。申請コンサルには勝てない）

### 4.4 判断が保留になっているもの

- **34217 を削除して新規出品として作り直すか。** 新着枠を取り直せるが、削除は取り消せないため未実施
- **リベシティの「つぶやき」に出品を載せるか。** 完了画面から載せられる。露出が最大の課題なので効果は期待できるが、
  SNS投稿にあたるため本人の許可を取ってから
- **34218（20,000円）と 34120（30,000円）を残すか。** 37160/37161 と領域が近く、
  カニバリの可能性がある。反応を見てから判断
- ~~古い3件の画像を作り直すか~~ → **2026-08-31 に実施済み。** 旧画像は `listings/assets/legacy/` にある

---

## 5. 作業のやり方

### ブラウザ操作

Claude in Chrome（`mcp__claude-in-chrome__*`）でログイン済みセッションを使う。

| 用途 | URL |
|---|---|
| サービス管理 | `https://skill.libecity.com/mypage/seller/services` |
| 新規出品 | `https://skill.libecity.com/services/new` |
| 出品の編集 | `https://skill.libecity.com/services/<id>/edit` |
| 検索 | `https://skill.libecity.com/search?parent_category=7&sort=new`（sort: `new` / `favorite` / 他） |

### 出品フォームの自動入力（今回確立した手順）

`javascript_tool` で `value` を設定し `input`/`change` を dispatch する。**3,000文字を `type` で打つのは非現実的。**

```js
const S=(id,v)=>{const e=document.getElementById(id);e.value=v;
  e.dispatchEvent(new Event('input',{bubbles:true}));
  e.dispatchEvent(new Event('change',{bubbles:true}));return e.value.length};
```

要素IDと値（実測）:

| 項目 | id | 備考 |
|---|---|---|
| 親カテゴリ | `parentCategoryForm` | IT・プログラミング = `7`。**change 後 1.5秒待つ**とサブが埋まる |
| サブカテゴリ | `subCategoryForm` | 77:作業自動化・効率化 / 78:Webアプリ / 81:サーバー・インフラ / 84:AI活用・開発 |
| タイトル | `titleInput` | 30文字 |
| キャッチコピー | `subTitleInput` | 100文字 |
| 詳細 | `detailInput` | 3,000文字 |
| 価格 | `price` | 数値 |
| お届け日数 | `skill_service_delivery_days` | 数値 |
| 無料修正 | `skill_service_free_modifying` | `no_modifying` / `1`〜`9` / `unlimited` |
| やりとり上限 | `valid_order_limit`（チェック）→ `order_limit_max` | click してから値を入れる |
| リベポイント払い | `skill_service_is_point_payment_enabled` | **必須。click でチェック** |
| 公開範囲 | `external_visibility_public` | **`public` = 「いいえ」（リベシティ内限定にしない）** |
| キャンセル注意 | `cancellationCautionInput` | 3,000文字 |
| スキル | `userSkillDescriptionInput` | 3,000文字 |
| ポートフォリオ | `portfolioInput` | **100文字**。URL 1本 |
| 依頼テンプレート | `orderTemplateInput` | 3,000文字 |
| 規約同意 | `termsCheck` | click が必要 |
| 確認へ進む | `confirmButton` | → `/services/confirm` → 「出品を開始する」 |

画像は `#uploadImageFiles`。`file_upload` ツールには **ref が必須**なので、
先に `find`（クエリ `upload_image_files`）で ref を取る。
`find` は「ファイルを選択」だと別の div を返すことがある。

**長文は JS のテンプレートリテラル（バッククォート）に実改行のまま入れる。**
原稿にバッククォートと `${` が無いことが前提。入力後に返る文字数を
`listings/*.md` の実測値と突き合わせて、欠けが無いことを確認する。

### 反映の検証

**公開後は必ず実際に取得して確認する。** `fetch` は Promise を待たないので同期 XHR を使う。

```js
(()=>{const ids=[37160,37161,37162];const bad=['Railway','Zapier','Power Automate','Airtable','Java SE 8'];
const out={};for(const id of ids){const x=new XMLHttpRequest();x.open('GET','/services/'+id,false);x.send();
const body=x.responseText.replace(/<[^>]+>/g,' ');out[id]={s:x.status,ng:bad.filter(b=>body.includes(b))}}
return out})()
```

**注意**: ページ下部の「関連サービス」に他人の出品が入るため、単純な文字列一致だと誤検知する。
ヒットしたら `raw.indexOf()` で前後を見て、自分の本文か確認すること。

### サムネイル画像の作り方

**これが標準の作り方。**（2026-08-31 に本人が承認。別のやり方に変えるのは本人が明示的にそう言ったときだけ）
**手順・落とし穴・デザイン方針はすべて [`listings/assets/README.md`](listings/assets/README.md) にある。作る前に読む。**
編集したいときは `listings/assets/pptx/*.pptx` を PowerPoint で開く。

新しい出品の画像を足すときは、`build-thumbnails.mjs` の `CARDS` に1件追加し、
`illust/gen-illust.mjs` に絵を1枚描き足す。

要点だけ:

- **AI画像生成は使えなかった。** Gamma はクレジット切れ（403 Insufficient credits）、Canva MCP は未接続。
  イラストは SVG で自作して透過PNG化している（`listings/assets/illust/`）
- 見出しフォントは **`HGP創英角ｺﾞｼｯｸUB`（半角カナ）**。全角で書くと LibreOffice が明朝に置換する
- `soffice --convert-to png` は1枚目のスライドしか出さないので、**1ファイル1スライド**にしている
- pptxスキルの `scripts/office/soffice.py` は Windows で動かない（`socket.AF_UNIX` が無い）。
  `soffice.exe` を直接呼ぶ。`validate.py` は `PYTHONUTF8=1` を付けないと正常なファイルでも FAILED になる

### 出品済みの画像を差し替える手順

編集画面の「削除」ボタンは `span.file_removeBtn` で幅0（ホバーで出る）ため、
`.click()` も座標クリックも効かない。**`li.selected_item` を DOM から消せばよい**
（画像の実体は中の `input.hiddenImage` で送信されるため）。

```js
document.querySelectorAll('li.selected_item').forEach(li => li.remove());
```

そのあと `find` で `upload_image_files` の ref を取り、`file_upload` で新しい画像を入れ、
`termsCheck` をチェックして `confirmButton` →「サービスを更新する」。

---

## 6. 触ってはいけないところ / 既知の落とし穴

- **`git stash` を裸で使わない。** ワークツリー間で stash スタックが共有されている
- **Git Bash の `/tmp` と Node の `C:\tmp` は別物。** ファイル受け渡しはリポジトリ内の相対パスで行う
- **34217 / 34218 / 34120 を消さない。** 判断保留中（4.3参照）
- 出品の削除は取り消せない。**本人の明示的な許可なしに削除しない**
- 「つぶやき」への投稿は SNS 投稿。**本人の許可なしに行わない**

---

## 7. 未確認のまま残っている項目

推測で埋めず、必要になったら実際に確認すること。

1. **手数料率** — 規約に記載が見当たらない。「使い方ガイド」（`/pages/guide`）で要確認
2. **リベポイント払いの精算方法** — 対応必須なのは確認済み。換金レート・振込タイミングは未確認
3. **検索の既定ソート「会員区分順」の影響** — イルカ会員が上位表示にどう効くかは未検証
4. **34120 の「Zapierは月額$49〜$100以上」** — 出典未確認の市場主張。指摘されると弱い
5. **新着一覧の先頭に固定で出る `33369`** — 広告枠と思われるが未確認

---

## 8. 参照先

| 何 | どこ |
|---|---|
| 設計の全体（根拠・競合分析・実測記録） | `docs/superpowers/specs/2026-08-14-libecity-skill-market-design.md` |
| 出品原稿（フォームに貼れる形） | `listings/01-ai-shindan.md`（34217）／`02-teiji-jikko.md`（37160）／`03-henkou-kenchi.md`（37161）／`04-gyomu-system.md`（37162） |
| サムネイル画像と生成スクリプト | `listings/assets/` |
| 本人の経歴・実績・技術スタックの一次情報 | `../my-data`（`03_business/clients.md`、`04_projects/products.md`、`04_projects/qa-status.md`、`04_projects/nexeed-ops.md`） |
| 両学長の「AI業務代行」の型 | `../libecity-library/docs/02g_ai-daiko.md`、受託型は `02i_client-work.md` |
