# 引き継ぎ — リベシティ スキルマーケット出品

最終更新: 2026-08-14

このファイルだけ読めば作業を再開できるようにしてある。
設計の根拠は [`docs/superpowers/specs/2026-08-14-libecity-skill-market-design.md`](docs/superpowers/specs/2026-08-14-libecity-skill-market-design.md)、
出品原稿は [`listings/`](listings/) にある。

---

## 1. 何をしているのか

リベシティ スキルマーケット（`https://skill.libecity.com`）への出品。

| 項目 | 内容 |
|---|---|
| **目的** | **受託案件の入口**。スキルマーケット単体の売上が目的ではない |
| 出口（受けたい案件） | ①中小企業の業務システム開発 ②事業者向けWebサイト＋AIチャットボット ③AI業務自動化の導入・伴走 |
| 稼働 | 週20時間以上 |
| 副業規定 | 確認済み。受託開発の出品に制約なし |
| 対応可能時間 | 平日 20:00〜23:00 ／ 土日祝 9:00〜12:00・13:00〜17:00・20:00〜23:00 |
| アカウント | イルカ会員。表示名「なおたか@沖縄の業務システム屋×AI」 |

### 設計の骨格（3層）

| 層 | 役割 | 価格帯 |
|---|---|---|
| 入口 | 診断書を納品する | 5,000〜6,000円 |
| 中間 | 小さく作って信頼を得る | 15,000〜50,000円 |
| 出口 | 見積型の開発 | 150,000円〜 |

**入口を診断書にしている理由**: 納品物が残るのでレビューが付き、非同期で完結して稼働を守れ、
「相談に乗る人」ではなく「調べて示す人」として認知される。
相談・伴走型を主軸にすると「教える人＝作らない人」と見られ、受託への導線が弱くなる。

---

## 2. 絶対に守る制約

### 規約: プラットフォーム外で受注してはいけない

利用規約（`/pages/rule`）の禁止事項:

> 出品者が利用者投稿情報によって、自己の商品やサービスあるいはSNS等メディアへ誘導し、
> **本サービスの機能を通さずに仕事を受託しようとする行為**。
> （対象サービスの補足説明のために外部メディアのURLを掲載する場合はこれに該当しない）

→ **診断書から Nexeed Lab の直契約に流すのは規約違反。** 高単価案件もスキルマーケット上の
出品として受注する。だから出口商品（⑥）を早く出す必要がある。

補足説明として `*.nexeed-lab.com` のURLを載せるのは明示的に除外されており問題ない。

その他: 既製品・汎用品の売買は禁止（ShiftCraft をそのまま売ることはできない。導入支援なら可）。
リモートで完結するサービスのみ出品可。

### 出品文には my-data で裏の取れる記載だけを載せる

**これが最重要。** 過去にAIで生成した出品文がそのまま公開されており、本人が「事実ではない」と
確認した記載が3件すべてに混入していた（2026-08-14 に是正済み。詳細は設計書 2.5節）。

技術名・実績・資格・数値は、ひとつずつ `../my-data` と突き合わせる。
裏付けが無ければ**勝手に補わず本人に確認する**。ツール名を「対応可能」として並べる誘惑に乗らない。

例外: 製品比較の文脈でツール名を出すのはスキルの主張ではないので問題ない
（34120 の「なぜZapierではなくn8nなのか」は残置している）。

### 出品文に実数を散りばめない

「13プロダクト」「362テスト」のような実数を各出品文に書くと、増減のたびに更新漏れが出て
古い数字が「実測済み」の顔で残る。数値は各商品につき1〜2個に絞り、あとは検証可能なURLを置く。

---

## 3. 現在の状態（2026-08-14 時点）

### 公開中の出品（3件・下書き0）

| ID | タイトル | 価格 | 状態 |
|---|---|---|---|
| [34217](https://skill.libecity.com/services/34217) | 【診断書納品】AIでやる事・やらない事を診断 | 5,000円 | **今回作成**。設計上の「商品①」 |
| [34218](https://skill.libecity.com/services/34218) | 問い合わせメールのAI自動返信を構築します | 20,000円 | 2026/03出品。スキル欄のみ是正 |
| [34120](https://skill.libecity.com/services/34120) | 毎日の作業をAIで丸ごと自動化 | 30,000円 | 2026/03出品。スキル欄のみ是正 |

34217 は元「業務自動化できる箇所を診断します」（1,000円）を全面的に書き換えたもの。
原稿は [`listings/01-ai-shindan.md`](listings/01-ai-shindan.md)、
画像は [`listings/assets/01-ai-shindan.png`](listings/assets/01-ai-shindan.png)
（Canva編集リンク: `https://www.canva.com/d/dF1NxLjjR-QFvMB`）。

### 完了済みの作業

- プロフィール表示名の変更（「なおたか@子育て奮闘中」→現在の名前）
- 34217 の全面書き換え・値上げ・カテゴリ移動・画像差し替え
- 34218 / 34120 のスキル欄の是正
- 下書き 36813 の削除

---

## 4. 次にやること（優先順）

### 4.1 診断書のテンプレートを作る ← 最優先

**34217 は「PDF・A4で10ページ前後の診断書」を約束しているが、実体がまだ無い。**
1件目の依頼が来る前に雛形を作る必要がある。構成は出品文に書いてあるとおり:

1. 業務フローの棚卸し（1枚の図）
2. AI・自動化で置き換えられる工程の特定と削減見込み時間の試算
3. やる順番の3分類（今すぐ1つ／3ヶ月後／**やらない方がいいもの**）
4. 道具の選定と理由（Claude Code / n8n / GitHub Actions / スプレッドシート）
5. 概算ランニングコスト

ヒアリングシート（Googleフォーム）も未作成。設問は出品の「依頼テンプレート」に書いた8問を流用できる。

### 4.2 フェーズ1の残り3商品を出す

設計では4商品でフェーズ1としていた。34217（①）以外は未着手。

| | 商品 | 価格 | カテゴリ | 備考 |
|---|---|---|---|---|
| ② | Webサイト・システムの技術診断書 | 6,000円 | IT・プログラミング > サーバー・インフラ | 完全非同期（URLだけで完結）。稼働を守れる |
| ③ | Windows で Claude Code を実運用する | 15,000円 | IT・プログラミング > AI活用・開発 | **競合最大手が Windows を明示的に断っている空白** |
| ⑥ | 業務システム開発（見積型） | 150,000円〜 | IT・プログラミング > Webアプリ | **これが無いと受託導線が規約内で完結しない** |

各商品の納品物・差別化の根拠・使える実測値は設計書4節に全部書いてある。

### 4.3 効果を測る

**測定の起点は 2026-08-14。** 34217 は閲覧7からの再スタート。

| 症状 | 原因 | 打ち手 |
|---|---|---|
| 閲覧が伸びない（2週間で50未満） | タイトル・画像・カテゴリ | タイトルを書き換えて再測定 |
| 閲覧はあるが気になるが付かない（200超で5未満） | 価格・内容の説得力 | 価格改定 or 納品物の明確化 |
| 気になるは付くが問い合わせが来ない | 依頼への導線 | 無料相談の入口を明示 |

比較対象（同カテゴリ・近い価格帯、2026-08-14 実測）:
カオマツ 5,000円で閲覧431・気になる24 ／ みずの 3,000円で239・21 ／ はるかぜ 5,000円で266・23。

参考として、既存3件は2026/03から4.5ヶ月で閲覧6〜13・やりとり0・取引0だった。

---

## 5. 作業のやり方

### ブラウザ操作

Claude in Chrome（`mcp__claude-in-chrome__*`）でログイン済みセッションを使う。
主要URL:

| 用途 | URL |
|---|---|
| サービス管理 | `https://skill.libecity.com/mypage/seller/services` |
| 新規出品 | `https://skill.libecity.com/services/new` |
| 出品の編集 | `https://skill.libecity.com/services/<id>/edit` |
| 利用規約 | `https://skill.libecity.com/pages/rule` |
| 表示名の変更 | `https://libecity.com/mypage/edit_profile` |

**長文フィールドは `javascript_tool` で `value` を設定し `input`/`change` イベントを
dispatch するのが速い**（Rails のプレーンフォームなので確実に効く）。
`computer` の `type` で3,000文字を打つのは非現実的。

フォームは 入力 → 確認 → 完了 の3段。新規出品には「下書きを保存する」があるが、
既存の編集にはない（確認画面から「サービスを更新する」で即公開）。

### 反映の検証

**公開後は必ず実際に取得して確認する。** 例:

```js
(()=>{const ids=[34217,34218,34120];const bad=['Railway','Zapier','Power Automate','Airtable','Java SE 8'];const out={};
for(const id of ids){const x=new XMLHttpRequest();x.open('GET','/services/'+id,false);x.send();
const body=x.responseText.replace(/<[^>]+>/g,' ');out[id]=bad.filter(b=>body.includes(b))}
return out})()
```

`fetch` は `javascript_tool` が Promise を待たないので使えない。同期 XHR を使う。

### フォームの制約（実測値）

| 項目 | 制限 |
|---|---|
| タイトル | 30文字 |
| キャッチコピー | 100文字 |
| サービスの詳細 | 3,000文字 |
| キャンセル時の注意事項 / スキル / 依頼テンプレート | 各3,000文字 |
| **ポートフォリオ** | **100文字**（URL 1〜2本が限度。一覧は詳細欄かスキル欄に書く） |
| 価格 | 最低500円。**リベポイント払いへの対応が必須**（チェック必須） |
| サービス画像 | 最大12枚 / png・jpg / 10MBまで / **3:2（660×440px）推奨**。リベ大素材はNG |
| 表示名（ニックネーム） | 20文字 |

### 画像の作り方

Canva MCP（`mcp__b43523be-...`）の `generate-design`（design_type: `postcard` が 3:2 に近い）
→ `create-design-from-candidate` → `edit-design` で修正 → `export-design`（png）→ `curl` で保存。

**AI生成の画像は必ず目視確認すること。** 4案のうち1案は日本語が全て豆腐（□□□）だった。
プレースホルダ（「[会社名]」）が残ることもある。
なお `export-design` に width/height を両方渡しても元デザインの縦横比が優先される（620×440 になった）。
実害は無かったが、3:2 ちょうどが必要なら `resize-design` を検討する。

---

## 6. 未確認のまま残っている項目

推測で埋めず、必要になったら実際に確認すること。

1. **手数料率** — 規約に記載が見当たらない。「使い方ガイド」（`/pages/guide`）で要確認
2. **リベポイント払いの精算方法** — 対応必須なのは確認済み。換金レート・振込タイミングは未確認
3. **検索の既定ソート「会員区分順」の影響** — ソート選択肢に存在することは確認済み。
   イルカ会員が上位表示にどう効くかは未検証
4. **34120 の「Zapierは月額$49〜$100以上」** — 出典未確認の市場主張。指摘されると弱い
5. **利用資格の「応援会員」にイルカ会員が該当するか** — 出品できているので該当すると推定

---

## 7. 参照先

| 何 | どこ |
|---|---|
| 設計の全体（根拠・競合分析・商品詳細） | `docs/superpowers/specs/2026-08-14-libecity-skill-market-design.md` |
| 出品原稿（フォームに貼れる形） | `listings/01-ai-shindan.md` |
| サムネイル画像 | `listings/assets/01-ai-shindan.png` |
| 本人の経歴・実績・技術スタックの一次情報 | `../my-data`（`03_business/clients.md`、`04_projects/products.md`、`04_projects/qa-status.md` が特に有用） |
| 両学長の「AI業務代行」の型 | `../libecity-library/docs/02g_ai-daiko.md`、受託型は `02i_client-work.md` |
